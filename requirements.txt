# ImgConverter runtime dependencies — pinned floors.
# Floors close the libheif/libjxl/Pillow CVE matrix documented in ROADMAP.md
# Appendix A6. Newer versions are fine; *older* versions of these are not.
#
# Required:
Pillow>=12.1.1
pillow-heif>=1.4.0
PyQt6>=6.8

# Optional — install only if you need that format family:
# numpy>=1.26                 # HDR tone mapping + PSNR quality target
# pillow-jxl-plugin>=1.3.6   # JPEG XL input + output
# rawpy>=0.27.0              # Camera RAW (CR2/CR3/NEF/ARW/DNG/...)
# qoi>=0.7                   # QOI input  (Pillow 11.3+ writes QOI natively)
